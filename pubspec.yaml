name: remindgo
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.3
  flutter_local_notifications: ^19.0.0
  timezone: ^0.10.1
  intl: ^0.20.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image_picker: ^1.0.7
  permission_handler: ^11.0.1
  file_picker: ^8.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  hive_generator: ^2.0.1
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "lib/assets/Remindgo_icon.png"
